# 开源 | 中继报时钟：从浏览器自动化到协议直连

**作者：BI9BZW**

---

## 起因

咸阳中继台 BR9AB 开通以来，不少友台反馈：希望中继台能在整点和半点自动播报时间、频率等信息，方便守听的 HAM 了解中继台状态。

最初的想法很朴素——**天猫精灵 + 一个手台**。天猫精灵负责定时播报，通过改装 AUX 接口输出音频，接到手台的麦克风输入，到点一念，中继台就能转发出去。天猫精灵我都下单了。

但转念一想：光是把这套方案跑通，就得改装天猫精灵的 AUX 口、焊接线材、调试手台音量和麦克风距离……硬件摆了一桌，能不能稳定工作还得听天由命。能不能**不用任何硬件**，纯软件搞定？

既然中继台已经接入了**滔滔链路**（ALLPTT），只要有电脑能打开网页、按下 PTT、对着麦克风说话，就能通过中继台发射。那问题就变成了：**怎么让程序代替人，自动完成"打开网页→按下PTT→播放语音→松开PTT"这一套动作？**

退掉天猫精灵，打开编辑器，开始写代码。

## 第一代：浏览器自动化

v1 版案的思路是**浏览器自动化 + TTS 语音合成 + 定时调度**：用 Playwright 无头浏览器打开滔滔链路的网页，用 JS 劫持 Web Audio API 注入"虚拟麦克风"（用音频文件替代真实麦克风输入），到点自动按下 PTT、播放语音、松开 PTT。

这套方案跑了几个月，期间持续打磨：播报前一分钟预热页面、TTS 音频像蓄水池一样提前合成好未来两天的量、播报期间零干扰……稳定性渐入佳境。

但浏览器方案有个与生俱来的软肋：**它模拟的是"人操作网页"，而网页本身不是我可控的**。

## 转折：网页网关故障

2026 年 8 月底，平台的网页网关故障了——网页对讲客户端连不上服务器，浏览器播报随之停摆，只能等平台修复。

有趣的是，排查时发现：**手机 App 一直好着**。网页和手机 App 走的是两条完全不同的路：网页走 Web 网关，手机 App 直连平台的核心协议端口。网页故障，协议端口纹丝不动。

那就干脆不等了——**既然手机 App 能直连，程序为什么不能？**

于是立项第二代方案：抛开浏览器，直接逆向平台的通信协议，让程序用和手机 App 同样的方式连上服务器。

## 第二代：协议直连

逆向的结论：平台的实时对讲服务器是一个**改造版 Mumble 服务器**（Mumble 是老牌开源语音对讲软件，协议公开）。平台的定制部分通过抓包手机 App 的真实流量逐字节比对确认，核心是四件事：

- **登录**：和服务器握手、报上账号
- **抢麦**：发送 ApplyMic 信令申请发言权
- **说话**：把语音编码成 Opus 音频流，按固定节奏发给服务器，服务器转发给频道内所有人（包括中继台链路设备，再发射到射频）
- **上报状态**：UserTalking 信令告诉服务器"我开始说话了/我说完了"——频 道 里其他人的手机上显示谁在讲话、服务器录音、中继台触发转发，都靠它

听起来简单，魔鬼全在细节里。直连版前后迭代了十几个版本，每个版本都踩出一个坑：

- 语音包头多写了一个字段，中继台收到的是纯噪声——那个字段是服务器转发时才加的"发件人标记"，上行根本不能带；
- 发包节奏照抄抓包看到的"每 600 毫秒一批"，结果手机和中继台收听都卡顿丢字——那其实是无线链路的聚包现象，真人说话时上行是匀速的，改成每 120 毫秒一包立刻干净了；
- 开头总是吞掉一两个字——中继台链路设备需要半秒建链时间，官方 App 按下 PTT 和开口说话之间天然有这个间隔，程序也得留；
- 服务器眼里这个账号显示为"浏览器登录"——客户端上报的版本信息里还写着"web"，对齐成真实手机 App 的画像才算严丝合缝。

最终架构：

```
定时调度 → 预热（TTS预合成+音频预构建+预建链路）→ 准点抢麦 →
匀速发包（120ms/包）→ 放麦 → 服务器回执确认
```

外加一条**常驻链路**：程序平时也挂在频道里，定期发心跳保活——在服务器眼里，它就是一个 7×24 在线的普通手机用户，而不是每半小时冒出来讲 30 秒话又消失的怪客户端。任何一环出问题，自动降级到备用路径，播报不中断。

## 直连版的效果

2026 年 8 月 30 日起在 BR9AB 上生产运行：

- **播报准时**：准点前 0.5 秒才抢麦（不提前占用信道），语音紧贴整点发出
- **链路可靠**：与手机 App 同级，不再受网页网关故障影响
- **端到端确认**：每次播报后服务器返回录音地址和时长，播没播成、播了多少秒，日志里一目了然
- **资源极省**：砍掉整个浏览器栈，容器镜像从 1.5GB 精简到约 300MB，主程序从 1100+ 行瘦身到 600 行；不播报时只有一个轻量心跳线程
- **异常告警**：企业微信推送错误告警；账号在别处登录被顶下线也会收到通知并自动重连

## 播报内容示例

> CQ CQ CQ，现在是2026年8月30日，星期日，14点30。这里是咸阳市业余无线电中继台 BR9AB，本中继下行频率 439.6875 兆赫，上行频率 434.4875 兆赫，叉频 负 5.2 兆赫。单上行接入亚音为模拟 88.5 赫兹。请规范用频，保持信道畅通。完毕

## 开源地址

项目已开源在 GitHub，采用 MIT 协议：

**https://github.com/MZYDJ/ham-repeater-bot**

欢迎各位友台 fork、star、提 issue。如果你也在用滔滔链路接入中继台，配置好账号和播报内容（Docker 环境变量即可），就能让你的中继台也拥有自动播报能力。

## 部署方式

项目支持 Docker 一键部署，可以在任何支持 Docker 的设备上运行：

- **NAS**：飞牛、群晖、威联通等，通过 Compose 图形界面导入即可
- **Linux 服务器**：`docker compose up -d` 一条命令搞定
- **软路由/小主机**：只要能跑 Docker 就行

直连版不需要浏览器、不需要声卡、不需要图形环境，对硬件几乎没有要求。详细的部署步骤和配置说明请参考项目 README。

## 后续计划

- [x] ~~支持更多链路平台~~ → 协议直连已实现，不依赖任何网页客户端
- [ ] 播报内容动态模板化（天气、温度等）
- [ ] 多中继台统一管理

## 致谢

感谢 BR9AB 管理组的支持，以及在测试过程中提供反馈的各位友台。

**73！**

**BI9BZW**
**2026年8月 于咸阳**

---

> 💡 **免责声明**：使用本项目前请确保你持有有效的业余无线电操作证书，中继台已依法取得电台执照，播报内容符合当地无线电管理规定。
